PROGRAM expr
BEGIN

	INT a,b;
	STRING newline := "\n";
	STRING B_BIGGER := "B is bigger!\n";
	STRING A_BIGGER := "A is bigger!\n";

	FUNCTION VOID main()
	BEGIN
        a := 1;
        b := 2;

        IF ( a < b )
            WRITE(B_BIGGER);
        ELSE
            WRITE(A_BIGGER);
        ENDIF

      WRITE(c, newline, d, newline, e, newline);

	END
END